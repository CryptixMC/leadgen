# ── Backend only (server-side — never expose these to the browser) ────────────
SUPABASE_URL=
SUPABASE_KEY=
GOOGLE_PLACES_API_KEY=
GOOGLE_PAGESPEED_API_KEY=
YELP_API_KEY=
FRONTEND_URL=
SCRIPT_API_TOKEN=   # generate with: python -c "import secrets; print(secrets.token_hex(32))"

# ── Frontend safe (PUBLIC_ prefix — Vite exposes these to the browser) ────────
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
